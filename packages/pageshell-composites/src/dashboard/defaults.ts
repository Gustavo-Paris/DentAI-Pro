/**
 * Dashboard Defaults
 *
 * Default configuration values for DashboardPage.
 *
 * @module dashboard/defaults
 */

// =============================================================================
// DashboardPage Defaults
// =============================================================================

/**
 * Default values for DashboardPage component
 */
export const dashboardPageDefaults = {
  theme: 'default' as const,
  containerVariant: 'shell' as const,
} as const;
