/**
 * Settings Defaults
 *
 * Default configuration values for SettingsPage.
 *
 * @module settings/defaults
 */

// =============================================================================
// SettingsPage Defaults
// =============================================================================

/**
 * Default values for SettingsPage component
 */
export const settingsPageDefaults = {
  theme: 'default' as const,
  containerVariant: 'shell' as const,
} as const;
