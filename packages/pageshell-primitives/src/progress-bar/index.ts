/**
 * PageProgressBar Module
 *
 * Simple progress bar component with color variants and sizes.
 *
 * @module progress-bar
 */

export { PageProgressBar } from './PageProgressBar';
export type {
  PageProgressBarProps,
  ProgressBarColor,
  ProgressBarSize,
} from './types';
