/**
 * Config Defaults
 *
 * Default configuration values for ConfigPage.
 *
 * @module config/defaults
 */

// =============================================================================
// ConfigPage Defaults
// =============================================================================

/**
 * Default values for ConfigPage component
 */
export const configPageDefaults = {
  theme: 'admin' as const,
  containerVariant: 'shell' as const,
} as const;
