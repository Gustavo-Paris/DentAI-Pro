/**
 * Detail Defaults
 *
 * Default configuration values for DetailPage.
 *
 * @module detail/defaults
 */

// =============================================================================
// DetailPage Defaults
// =============================================================================

/**
 * Default values for DetailPage component
 */
export const detailPageDefaults = {
  theme: 'default' as const,
  containerVariant: 'shell' as const,
  backLabel: 'Back',
  stickyFooter: false,
} as const;
