/**
 * Linear Flow Defaults
 *
 * Default configuration values for LinearFlowPage.
 *
 * @module linear-flow/defaults
 */

// =============================================================================
// LinearFlowPage Defaults
// =============================================================================

/**
 * Default values for LinearFlowPage component
 */
export const linearFlowPageDefaults = {
  theme: 'default' as const,
  containerVariant: 'shell' as const,
  backLabel: 'Back',
  nextLabel: 'Continue',
  showFooter: true,
} as const;
