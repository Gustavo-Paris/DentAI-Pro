/**
 * Calendar Defaults
 *
 * Default configuration values for CalendarPage.
 *
 * @module calendar/defaults
 */

// =============================================================================
// CalendarPage Defaults
// =============================================================================

/**
 * Default values for CalendarPage component
 */
export const calendarPageDefaults = {
  theme: 'admin' as const,
  containerVariant: 'shell' as const,
} as const;
